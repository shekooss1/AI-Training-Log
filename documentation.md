1. Kernel : is part between operatin system and hardware to run programs .

2. docker : wrapp my database or my program in a container so can be shipped anywhere
   # So postgre run inside docker cotainer and this container needs linuks to run that is why I installed linux kernel on my device

3.  CI : check runs and tests and make sure build  not broken 
    CD : publis the code on server after CI finish its work     

4. Hibernate : tool java use to turn classes into DB tables 

5. .gitignore : tells github to ignores secret files or folder in it 
     
     # .env : passwords and API key